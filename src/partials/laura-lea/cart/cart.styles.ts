import styled from '@emotion/styled';

export const Container = styled.div`
  position: relative;
  min-height: 80vh;
  background: var(--grey-light);

  component-cart {
    max-width: 500px;
    margin: 0 auto;
  }
`;
