import React, { FC } from 'react';

import { Layout } from 'components';

const projectTitle = 'HKFD';

const HKFD: FC = () => <Layout projectTitle={projectTitle}>Content</Layout>;

export default HKFD;
